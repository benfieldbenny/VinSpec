#!/bin/bash
# Activate venv first, then run:
# uvicorn app:app --reload --port 8000
# In another terminal: python agent.py
echo "Run: uvicorn app:app --reload --port 8000"
echo "Then: python agent.py"
